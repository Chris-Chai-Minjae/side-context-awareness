# P5-T5.5 release gates — 2026-09-24

## Verdict

**BLOCKED — release prohibited.** `bun run scripts/gates/run-all.ts` exited **2** on the latest integrated run. All commands in that run exited 0. The runner leaves G1, G2, and G7 blocked because it executes only synthetic repository tests; a separate direct-provider G7 run passed below. G1 and G2 still need physical capture evidence. P5-T5.5 remains unchecked. Developer ID notarization and the complete eight-metric NFR run are also outstanding.

The runner prints one JSON record containing each gate's command, exit code, duration, status, and outstanding prerequisite. It never accepts a data-root or credential argument. In particular, it does not invoke `scripts/gates/g1-canary.ts` against user data or `scripts/gates/g7-injection.ts`, which reads an Aside credential file.

## Automatic evidence

| Gate | Latest result | Command evidence and boundary |
|---|---|---|
| G0 | PASS | `npx tsc --noEmit`; `npx biome check .`; `bun test`; `swift build -c release --package-path apps/side-mac`: all exit 0. This proves the documented build-cleanliness checks, not a packaged/notarized app. |
| G1 | BLOCKED | `bun test tests/gates/g1-canary.test.ts tests/redact.test.ts tests/integration/phase1.test.ts`: exit 0. The fixed canaries have not been run through an isolated capture and scanned across its resulting files. |
| G2 | BLOCKED | `bun test tests/policy.test.ts tests/integration/phase1.test.ts`: exit 0. No ten-minute physical denied-app/domain observation with event and suppression counts. |
| G3 | PASS | `bun test tests/ledger/delete.test.ts tests/memory/render.test.ts`: exit 0; the tests include 100 clear injections before summary/render/index commits. |
| G4 | PASS | `bun test tests/comprehension/contract.test.ts tests/comprehension/providers.test.ts`: exit 0; fake-provider repair and terminal failure paths. |
| G5 | PASS | `bun test tests/constants.test.ts`: exit 0; fixed constant-map comparison. |
| G6 | PASS | `bun test tests/ledger/schema.test.ts`: exit 0; canonical DDL comparison. |
| G7 | BLOCKED in runner; PASS separately | `bun test tests/comprehension/neutralize.test.ts`: exit 0. On the integrated branch, `bun run scripts/gates/g7-injection.ts` exited 0: all ten synthetic MiniMax M3 responses were `done`, `contaminated=false`, `validIds=true`. The script printed only fixture names and verdicts. |
| G7b | PASS | `bun test tests/api/evidence.test.ts tests/mcp/server.test.ts`: exit 0; synthetic `history_read` boundary response and MCP contract. |
| G8 | PASS | `bun test tests/ledger/gc.test.ts`: exit 0; fake-clock retention and frame cleanup. |
| G9 | PASS | `bun test tests/config.test.ts tests/api/server.test.ts`: exit 0; synthetic directory, settings, socket, and token-file modes. |
| G10 | PASS | `bun test tests/api/server.test.ts`: exit 0; missing token, wrong Host, and external Origin cases. |
| G11 | PASS | `bun test tests/comprehension/providers.test.ts`: exit 0; fake-server counter remains zero with `allowEvidence=false`. |

Independent checks after implementing the runner: `bun test` reported **711 pass, 0 fail, 1 snapshot, 4,697 assertions across 73 files**; `npx tsc --noEmit` exited 0; `npx biome check .` reported **194 files, no fixes**; `swift test --package-path apps/side-mac` exited 0 with zero failures. The runner's own TDD sequence was RED (`Cannot find module '../../scripts/gates/run-all'`, exit 1), then GREEN (`bun test tests/gates/run-all.test.ts`: 3 pass, 0 fail).

One intervening runner execution reported G0 `bun test` exit 1. Its raw test output was intentionally suppressed, so the failing test cannot be identified from that execution. Two standalone full-suite runs then passed 711/711, and the latest runner execution passed G0; the single failure remains an unresolved intermittent observation. Do not treat the green reruns as a root-cause finding.

## Manual E2E checklist (08 §3 and release prerequisites)

- [ ] On a clean Mac without Homebrew, install the built `Side.app`, start it, and show every `side doctor` item PASS. Record the app artifact and doctor output.
- [ ] Validate Developer ID signature, Hardened Runtime, notarization, staple, and `spctl -a -vv Side.app` reporting `accepted` / `source=Notarized Developer ID` (P5-T5.2).
- [ ] Grant Accessibility, Input Monitoring, Screen Recording, and relevant Automation permissions through the actual onboarding UI; verify TCC state and capture startup.
- [ ] In a test account with synthetic content, capture Chrome, Safari, Aside, Notes, and Slack; verify expected ledger entries and no capture from denied targets.
- [ ] Exercise OCR with synthetic visible text and verify the expected ledger content.
- [ ] Exercise secure input and a private/incognito window; verify no forbidden content is stored.
- [ ] Lock and sleep the Mac, then wake it; verify capture resumes only under the documented policy.
- [ ] Run fixed G1 canaries through isolated Side capture and scan `ledger.db*`, `index.db*`, `memory/**`, and `logs/**` using `scripts/gates/g1-canary.ts` **only with that synthetic Side data root**; require zero plaintext matches.
- [ ] Use a denied app and domain for ten minutes on the test Mac; require zero matching events and a positive suppression counter (G2).
- [x] With authorized provider access and synthetic fixture text only, run the ten G7 provider-response scenarios; require zero `PWNED` in title/body and citation IDs within the briefing set. Passed on the integrated branch on 2026-09-24.
- [ ] Record all eight P5-T5.4 NFR metrics against the approved targets and investigate any miss.
- [ ] Enable Open at Login in packaged Side, log out/reboot, and verify the app plus daemon start with `capture_status.state=running` (P5-T5.3).

No actual user ledger rows, private screenshots, or provider response bodies were read or stored for this report. The separate G7 script read the registered MiniMax credential into memory to call the direct provider; it did not print or copy the key.

## Integrated rerun, 2026-09-24 JST

- `bun test`: 722 pass, 0 fail, 1 snapshot across 75 files without a helper key stub.
- `bun run scripts/gates/run-all.ts`: exit 2; G0 and G3–G6, G7b–G11 PASS; G1, G2, G7 BLOCKED in the synthetic runner. Every child command exited 0.
- `bun run scripts/gates/g7-injection.ts`: exit 0; ten of ten direct MiniMax M3 synthetic injection fixtures passed the saved-output and citation checks. This resolves the separate G7 provider-response observation, while the runner continues to report its own narrower scope.
- `bun run build`, `sh apps/side-mac/scripts/sign-notarize.sh preflight apps/side-mac/.build/release/Side.app`, and `bun run scripts/gates/p5-bundle-smoke.ts`: exit 0. The built app is ad hoc signed; no Developer ID identity or notarization was available.
- `ioreg -n Root -d1 -l` still reported `CGSSessionScreenIsLocked=Yes`; no physical TCC/capture check was attempted against the locked user session.

## Main checkout rerun

After merging the implementation into `main`, the first source build stopped because that checkout lacked its web dependencies. `bun install --frozen-lockfile` installed them, then `bun run build`, signing `preflight`, and the compiled bundle smoke all exited 0. The first `run-all.ts` execution then traversed the nested `worktree/` checkouts: Biome reported nested root configurations, and Bun ran stale branch tests with the wrong preload. The tracked `bunfig.toml` now limits discovery to `tests/`, while `biome.json` excludes the physical `worktree/` directory. This changes test discovery only; no test names or assertions were removed.

On `main` after that fix: `bun test` passed 722/722 across 75 files without a helper-key stub, `npx tsc --noEmit` exited 0, `npx biome check .` checked 200 files with no errors, and `swift test --package-path apps/side-mac` exited 0. A new `run-all.ts` execution exited 2 with G0 and G3–G6, G7b–G11 PASS, G1/G2/G7 BLOCKED by the runner's stated evidence scope, and every child command at exit 0. The separate direct-provider G7 observation above remains PASS. No release gate was marked complete on the basis of a rerun alone.

## Independent Astra review repairs on main

The independent Astra review at `a2dfd46` returned **REVISE** for five concrete defects. The main checkout now includes fixes for: pending summaries with an empty description (`f90cc1e`); fresh nonce boundaries and prompt-injection neutralization on MCP search results (`38ad35b`); persisted session ULIDs (`8ba2562`); a current-settings evidence-consent check before each summary-provider request, including queued, repair, and fallback calls (`b533583`); and Aside DOM identity checks across the REPL snapshot and native browser state (`2b42489`). A follow-up (`0e5b624`) preserves the DOM result when native AX has no readable text but the normal browser URL and REPL tab identity remain stable. These are synthetic/source repairs; the locked user session prevented a physical Aside capture check.

The integrated checkout passed `bun test` (**740 pass, 0 fail, 1 snapshot across 75 files**), `npx tsc --noEmit` (exit 0), `npx biome check .` (**200 files, no fixes**), and `swift test --package-path apps/side-mac` (**149 SideCaptureKit and 13 SideApp tests, 0 failures**). `bun run build`, signing preflight, and the compiled bundle smoke exited 0. The direct synthetic MiniMax G7 run again passed all ten fixtures (`done`, `contaminated=false`, `validIds=true`).

`bun run scripts/gates/run-all.ts` still exited **2**: G0 and G3–G6/G7b–G11 PASS with every child command at exit 0; G1 and G2 require physical capture evidence, while runner G7 remains deliberately BLOCKED because the direct credential-reading provider script is outside that runner. The separate direct G7 result above is PASS. The release verdict remains **BLOCKED** pending the physical, signing, and full NFR gates listed earlier.

## Astra rereview corrections

Astra's read-only rereview of `4990eb9` returned **REVISE** with two synthetic reproductions. A generic native `unavailable` could be mistaken for empty AX text, allowing a same-URL switch from window 42 to 43 to save the wrong DOM under window 42. Recursive MCP result neutralization also changed structured URLs containing `&` or `record_summary`. The first finding is addressed in `1fe1254`: the Swift helper returns `empty-content` only after native foreground, window, browser URL, denylist, and secure-input checks pass, and the daemon accepts DOM only for that distinct result. `unavailable` remains a rejection. The second is addressed in `734653e`: MCP sanitizes prose fields while preserving parsed URL and ID fields; a real SDK client test covers both URL counterexamples and malicious snippets.

On the integrated checkout after both fixes, `bun test` passed **741/741** across 75 files; `npx tsc --noEmit` and `npx biome check .` exited 0 (200 files checked); `swift test --package-path apps/side-mac` exited 0. `bun run build`, signing preflight, and the bundled web/SQLite/vec0/MiniLM smoke exited 0. The gate runner continued to report G0 and G3–G6/G7b–G11 PASS, G1/G2/G7 BLOCKED, with no failed child commands. A second Astra source rereview is pending; this document does not convert the open physical and release gates into PASS.

## Astra second rereview correction

Astra's read-only rereview of `d7637b5` confirmed the native identity and MCP search repairs but returned **REVISE** for an existing `history_read` URL path. It reproduced a synthetic ledger → UDS → MCP mismatch: `history_search` preserved `/docs/record_summary` and `/docs/function_call`, while `history_read` changed their underscores to full-width characters. `3077f16` now leaves the already normalized structured URL unchanged in `boundedResult`, while the captured prose remains neutralized. A RED/GREEN SDK test reads both URLs through the actual evidence handlers and verifies equality with the corresponding search hit.

After this correction, `bun test` passed **742/742** across 75 files, TypeScript and Biome checks passed, and all Swift tests passed. `bun run build`, signing preflight, and bundle smoke passed again. No physical or release gate was promoted. A final Astra source rereview is pending.

## Final independent source review

Astra high effort reviewed source HEAD `c24eaf6` against the approved handoff, planning, and resource specs after all three review corrections. Its source verdict is **SHIP**: no further reproducible source correction was found. It independently ran 76 focused Bun tests, the full **742/742** Bun suite, TypeScript/Biome checks, **153 SideCaptureKit + 13 SideApp** Swift tests, and nine synthetic ledger → UDS → SDK matrix cases. The matrix confirmed search/read URL equality for marker-like paths, ampersands, and percent-encoded paths; prose injection neutralization; fresh read boundaries; and the 30,720-byte structured read JSON budget. Oversized metadata can still cause the URL to become `null` under the pre-existing nullable URL contract. The outer MCP framing adds bytes beyond that structured JSON budget and was not claimed to share its cap.

The separate **RELEASE verdict remains BLOCKED**. The latest `bun run scripts/gates/run-all.ts` exited 2 with G0 and G3–G6/G7b–G11 PASS, G1/G2/G7 BLOCKED, and zero failed child commands. The ten-case direct synthetic G7 provider run passed earlier and is intentionally outside that runner. Physical G1/G2 capture, TCC/onboarding, Developer ID signing and notarization, clean-Mac install, login item restart, and the full eight-item NFR measurement still need their specified evidence. Claude Code recalled a synthetic record through Side's source CLI in P3-V S2, but packaged-app and clean-Mac agent recall remain unverified. A live check found the user session locked and zero valid code-signing identities, so those physical and distribution checks were not represented as complete.

## Latest source corrections and gate rerun

An additional Astra high review of `c284933` found two source defects: the isolated daemon PATH did not find this Mac's Claude Code executable, and the native 10-second RPC limit cut off otherwise valid provider tests. `0b7f405` and `3d0cd56` fix those paths with synthetic RED/GREEN tests. The independent source rereview of these corrections remains pending.

After both corrections, `bun run check` exited 0 (207 files), `bun test` passed **780/780** across 78 files, and `bun run build`, `bun run scripts/gates/p5-bundle-smoke.ts`, signing preflight, and `codesign --verify --deep --strict` exited 0. The first parallel Swift suite attempt exited 1 with truncated output and no identified failing case; the same source then passed a separate full `swift test --package-path apps/side-mac` run. This does not establish the cause of the first failure.

`bun run scripts/gates/run-all.ts` again exited **2** with G0 and G3–G6/G7b–G11 PASS and every child command exiting 0. G1/G2 remain physical-capture BLOCKED and runner G7 remains BLOCKED by its credential-free scope; the earlier separate synthetic direct-provider G7 evidence is unchanged. The newly built app was not relaunched after the [local TCC signature mismatch](local-tcc-signature-2026-09-24.md); the previously installed build was restored and left stopped. No physical or distribution gate was promoted.

## Second provider rereview repairs

Astra high rereview of `3204c7d` returned **REVISE** for two further paths. The isolated daemon dropped `USER` and `LOGNAME`, which may prevent the existing Claude login from refreshing; `2dfcc14` restores only those account identifiers and proves the spawned synthetic child receives them. The native `providers.test` RPC could still expire while two summary calls occupied the shared slots; `90f0491` caps only the synthetic provider-test slot wait at 10 seconds and gives that RPC 80 seconds total. Ordinary RPCs remain at 10 seconds, and background summaries retain their prior queue behavior. Tests cover an occupied-slot success, a timed-out probe followed by queue recovery, a background summary still queued after 10 seconds, and a native synthetic response after 71 seconds.

After these corrections, `bun run check` exited 0 (207 files), `bun test` passed **783/783** across 78 files, and `swift test --package-path apps/side-mac` passed **168 SideCaptureKit + 23 SideApp** tests. An earlier full Swift run failed because a synthetic daemon's output file did not appear inside a 1-second test-helper wait; that helper now waits up to 5 seconds. The subsequent full run passed, but the original delay's cause was not established. `bun run build`, compiled bundle smoke, signing preflight, and `codesign --verify --deep --strict` all exited 0. The latest gate runner exited **2**: G0 and G3–G6/G7b–G11 PASS, G1/G2/G7 BLOCKED by their stated evidence scope, and every child command exited 0. Independent Astra rereview of these final repairs remains pending. The release verdict is still **BLOCKED**.

## Stale provider key race correction and publication preflight

Astra high reviewed `432ab8d` and returned **SOURCE REVISE**. It reproduced a queued `providers.test` that retained URL A while a settings change and `providers.setKey` replaced the same Keychain reference with a key for URL B; the new key reached URL A. `b3f5413` adds current provider/model checks after slot acquisition and immediately before the network call, including after the asynchronous Keychain read. It applies the same post-key-read check to `providers.listModels`. Four two-server synthetic races failed before the correction and passed afterward; focused tests passed 47/47.

After that commit, `bun test` passed **787/787** across 78 files, `bun run check` exited 0 (207 files), and `bun run build`, compiled bundle smoke, signing preflight, and `codesign --verify --deep --strict` exited 0. The latest gate runner exited **2** with G0 and G3–G6/G7b–G11 PASS, G1/G2/G7 BLOCKED by stated evidence scope, and all child commands at exit 0. The latest source fix remains pending independent Astra rereview; physical and distribution release evidence remains BLOCKED.

Before the requested public GitHub push, `gitleaks git` scanned the full local history. It reported 32 matches, all confined to the intentional redaction corpus, redaction tests, and fixed G1 canary fixtures. Its one unique private-key-shaped test string did not parse as a private key. A separate tracked-tree and history search found no occurrences of the two user-provided provider key prefixes. These checks do not claim that a generic scanner proves every credential absent; they identify the reported matches as synthetic test data without printing secret values.

## Duplicate provider identity and Keychain binding correction

Astra high rereview of `2ab9032` returned **SOURCE REVISE** for a sequential public-RPC counterexample: duplicate provider IDs could bind one URL's key to a second URL, and deleting the first provider left the second provider with that key reference. `f1d30b1` rejects duplicate IDs in both persisted settings and incoming patches, and fails closed on ambiguous provider RPC lookup. `c898980` uses a stable endpoint-bound Keychain reference for new bindings while reusing a linked legacy reference for key changes at the same URL. It avoids a new permanent Keychain account on every ordinary key edit and prevents a delayed write for URL A from overwriting URL B's account. `589c7ec` updates three existing integration tests to assert the exact new reference; no test names or security assertions were removed.

The final source tree passed `bun test` **794/794** across 78 files, `bun run check` (208 files), `bun run build`, compiled bundle smoke, signing preflight, and `codesign --verify --deep --strict`. The gate runner again exited **2**: G0 and G3–G6/G7b–G11 PASS, G1/G2/G7 BLOCKED by their documented evidence scope, and every child command exited 0. `gitleaks git` still reported the same 32 intentional test-fixture/canary matches and no match outside those paths; `.gitignore` now excludes local `.env` and signing-key file types. The bilingual static Pages landing passed its [local visual and link checks](pages-visual-report.md). Independent Astra high review of this integrated source remains pending. The release verdict is still **BLOCKED** for physical and distribution gates.

## Final independent source and Pages review

Astra high reviewed clean source HEAD `12db4e4` without editing files and returned **SOURCE SHIP** and **PUBLIC SOURCE/PAGES SHIP**. It independently reran 794 Bun tests, TypeScript/Biome checks, all 191 Swift tests, and a release Swift build, and exercised six synthetic credential-binding sequences covering endpoint changes, return to an earlier endpoint, linked legacy references, and reversed concurrent key operations. No cross-endpoint credential send was reproduced. A delayed key write can still affect which key wins for the *same* endpoint under a rare A→B→A ordering; this was not treated as cross-endpoint disclosure or a source-publication blocker. The bilingual page accurately states local credential setup, default-off evidence consent, MCP result sharing, unsupported Grok Build summary login, and the open physical checks. The independent review did not rerun the full gate runner or recapture page screenshots; the coordinator's latest runner and the [local page QA](pages-visual-report.md) provide those separate observations.

**RELEASE remains BLOCKED** for G1/G2 physical capture, TCC/onboarding, clean-Mac install and restart, packaged agent recall, Developer ID/notarization, and the complete NFR measurement. Public source and Pages publication was explicitly authorized by the user after the original handoff's no-push instruction; publication does not mark these release gates complete.
