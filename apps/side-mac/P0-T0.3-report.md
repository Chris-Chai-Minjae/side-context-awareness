# P0-T0.3 SwiftPM skeleton handoff

Status: **acceptance verified**. The coordinator selected `com.minjaechai.Side` as the Bundle Identifier and reverified the package after this report's initial handoff.

## Delivered

- `Package.swift`: SwiftPM package `SideCaptureKit`, a pure-logic library, `Side` executable, and `SideCaptureKitTests`; macOS 14 minimum.
- `Sources/SideCaptureKit/Protocol.swift`: documented helper protocol version `1`.
- `Sources/Side/SideApp.swift`: minimal SwiftUI `MenuBarExtra` with a Quit action; no capture or permissions code.
- `Info.plist`: `LSUIElement=true`, macOS 14 minimum, Bundle Identifier `com.minjaechai.Side`.
- `scripts/build-app.sh`: packages the release executable as `.build/release/Side.app`.
- `biome.json`: excludes SwiftPM's generated `.build` directory from the root Biome check.
- `Tests/SideCaptureKitTests/ProtocolTests.swift`: verifies the documented protocol version.

`native/observe.swift` and `native/key.swift` were unchanged. No files outside `apps/side-mac` were edited for this task; no Git staging or commit was done.

## Verification

| Command | Result |
| --- | --- |
| `swift test --package-path apps/side-mac` | exit 0; 1 test passed, 0 failures. An initial RED run failed because `CaptureProtocol.version` did not exist; it passed after implementation. |
| `swift build --package-path apps/side-mac -c release` | exit 0; `Build complete!` |
| `bun test` | exit 0; 28 passed, 0 failed, 67 assertions across 12 files. |
| `npx tsc --noEmit` | exit 0; no diagnostics. |
| `npx biome check .` | exit 0; checked 32 files, no fixes. The first run found 17 format errors in generated `.build` JSON; the scoped `biome.json` exclusion resolved them. |
| `apps/side-mac/scripts/build-app.sh` | exit 0; created `.build/release/Side.app`. |
| `plutil -lint apps/side-mac/Info.plist` | `OK`. |
| `plutil -extract LSUIElement raw -o - apps/side-mac/.build/release/Side.app/Contents/Info.plist` | `true`. |
| `open -n apps/side-mac/.build/release/Side.app` | exit 0; `pgrep` and `lsappinfo` showed the running `Side` app; the launched process was then terminated. |
| AX menu bar probe after `open -n` | `sideRunning=true`, `AXExtrasMenuBar` returned success with 1 item, and a menu bar item carried the `Side` label. The launched test process was terminated afterward. |

The macOS Accessibility tree confirms a `Side` status item exists; its pixels were not inspected. No screen capture, sensitive-data access, or permission request occurred.
